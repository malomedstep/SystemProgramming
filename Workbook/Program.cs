using System;
using System.Collections.Generic;
using System.Threading;
using System.Collections.Concurrent;
using System.Threading.Tasks;
using System.Net;
using System.Net.Http;
using System.Reflection;

namespace Workbook {
    public class NotProgram {

    }
    public struct Atata {

    }
    public enum NotAtata {

    }
    public class Program {
        static void Main(string[] args) {
          //   Assembly asm = Assembly.Load(
          //     AssemblyName.GetAssemblyName("TPL_WPF.exe")
          //  );
          //   var type = asm.GetType("TPL_WPF.Class1");
          //   
          //   var obj = Activator.CreateInstance(type) as IComparable;
           




            //Assembly asm = Assembly.Load(
            //   AssemblyName.GetAssemblyName("TPL_WPF.exe")
            //);
            //var type = asm.GetType("TPL_WPF.Class1");
            //var methodInfo = type.GetMethod("Foo");
            //var obj = Activator.CreateInstance(type);
            //methodInfo.Invoke(obj, null);

            // var obj = new Class1();
            // obj.Foo();




            //Assembly asm = Assembly.Load(
            //    AssemblyName.GetAssemblyName("TPL_WPF.exe")
            //);
            //var types = asm.GetTypes();

            //foreach (var item in types) {
            //    Console.WriteLine(item.FullName);
            //    foreach (var m in item.GetMethods()) {
            //        Console.WriteLine($"\t{m.Name}");
            //    }
            //}
        }
    }
}
